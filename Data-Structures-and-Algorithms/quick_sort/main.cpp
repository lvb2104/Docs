#include <bits/stdc++.h>
using namespace std;

using ll = long long;

/**
 * quickSort(A, p, r)
 * 1. if (p < r){
 *      p = partition(A, p, r)
 *      quickSort(A, p, q - 1)
 *      quickSort(A, q + 1, r)
 * }
*/

// Hoare
// Lomuto
// pivot

int main(){
    // #ifndef ONLINE_JUDGE
    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);
    // #endif
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    return 0;
}