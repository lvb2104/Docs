maxEle)
        // {
        //     v.push_back(a[i]);
        //     maxEle = a[i];
        // }
        // else
        // {
        //     auto it = lower_bound(v.begin(), v.end(), a[i]);
        //     int pos = it - v.begin();
        //     v[pos] = a[i];
        //     if (pos == v.size() - 1)
        //         maxEle = a[i];
        // }